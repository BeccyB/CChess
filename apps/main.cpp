#include <iostream>
#include "controller/controller.h"

int main() {

    controller::Controller controller;

    controller.run();

    // NEXT TIME
    // clean up
    // use STL library algorithm

    return 0;
}